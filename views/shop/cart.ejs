<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <main>
            <% if (productData.length) { %>
                <ul>
                    <% productData.forEach(({ product, qty }) => { %>
                        <li><%= product.title %> (<%= qty %>)</li>
                    <% }) %>
                </ul>
            <% } else { %>
                <h1>No products in cart!</h1>
            <% } %>
        </main>
        <%- include('../includes/end.ejs') %>